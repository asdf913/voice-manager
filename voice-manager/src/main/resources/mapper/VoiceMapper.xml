<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.VoiceMapper">
	<insert id="insert">
		<if test="voice!=null">
			insert into
			voice(text,romaji,file_path,file_length,file_digest_algorithm,file_digest)values(#{voice.text},#{voice.romaji},#{voice.filePath},#{voice.fileLength},#{voice.fileDigestAlgorithm},#{voice.fileDigest});
		</if>
	</insert>
</mapper>